services:
  site:
    image: jekyll/jekyll:4
    command: sh -c "gem install webrick && jekyll serve --watch --force_polling --host 0.0.0.0 --port 4000"
    ports:
      - "4000:4000"
    volumes:
      - .:/srv/jekyll
    environment:
      JEKYLL_ENV: development
